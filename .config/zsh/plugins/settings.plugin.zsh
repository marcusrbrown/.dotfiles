#!/usr/bin/env zsh
# shellcheck disable=SC1071

setopt append_history         # append to the history file across all shells
setopt inc_append_history     # write to the history file immediately, not when the shell exits
