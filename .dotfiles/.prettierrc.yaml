printWidth: 120
singleQuote: true
trailingComma: none

# Leave fenced code blocks in Markdown exactly as written. The TypeScript in
# docs/solutions/ mirrors real scripts that Prettier does not format, so letting
# it restyle those samples would make the docs contradict the code they describe.
embeddedLanguageFormatting: off
